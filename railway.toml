[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 30
startCommand = "yarn start:railway"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
